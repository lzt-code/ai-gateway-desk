{
  "name": "ai-gateway-desk-worker",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker 部署模块 — AI Gateway 转发封装层",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  }
}
